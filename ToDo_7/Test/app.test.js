
test("Retorna o resultado da soma entre dois valores",()=>{
    expect(somar(3,3)).toBe(6)
 })

 test("Retorna o resultado da subtração entre dois valores",()=>{
    expect(subtrair(2,1)).toBe(1)
 })

 test("Retorna o resultado da multiplicação entre dois valores",()=>{
    expect(multiplicar(4,2)).toBe(8)
 })

 test("Retorna o resultado da divisão entre dois valores",()=>{
    expect(dividir(2,2)).toBe(1)
 })